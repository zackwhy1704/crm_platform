import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export default function Page() {
  return (
    <PlaceholderPage
      title="AI Qualification Calls"
      subtitle="WhatsApp-first in M1 — voice AI added in M2"
      note="M1 uses text-based WA conversations via Claude Haiku. Voice AI (Retell + ElevenLabs) is deferred to M2 as an escalation path for hot leads."
    />
  );
}
